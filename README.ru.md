<p align="center">
  <img src="docs/assets/hero.jpg" alt="Цербер: трёхголовый пёс над огненными вратами, одна голова зелёная, одна жёлтая, одна красная" width="100%">
</p>

<p align="center">
  <a href="https://github.com/concordloom/cerberus/actions/workflows/ci.yml"><img src="https://github.com/concordloom/cerberus/actions/workflows/ci.yml/badge.svg" alt="CI"></a>
  <img src="https://img.shields.io/badge/license-MIT-blue" alt="MIT">
  <img src="https://img.shields.io/badge/Claude%20Code-plugin-8a6cff" alt="Плагин Claude Code">
  <img src="https://img.shields.io/badge/Codex-skill-10a37f" alt="Навык Codex">
</p>

# cerberus

**Проверка для ИИ-агентов, которая не верит на слово.** Прежде чем агент
скажет вам, что изменение работает, он обязан всерьёз попытаться доказать, что
оно сломано, — и не суметь.

[English version](README.md) · [Сам навык](plugins/cerberus/skills/cerberus/SKILL.ru.md) ([en](plugins/cerberus/skills/cerberus/SKILL.md))

## Быстрый старт

**Claude Code** — две команды:

```
/plugin marketplace add concordloom/cerberus
/plugin install cerberus@concordloom
```

**Codex** — одна команда, прямо в сессии:

```
$skill-installer install https://github.com/concordloom/cerberus/tree/main/plugins/cerberus/skills/cerberus
```

Замените `cerberus` на конце на `critic` или `setup`, чтобы добавить два других.

**Или положите файлы в свой репозиторий** — если навыки нужны всей команде и
сборке, а не только вам: два способа выше ставят их лично вам, этот кладёт их в
проект, откуда их можно закоммитить. Клонировать нечего, повторный запуск
безопасен:

```console
curl -fsSL https://raw.githubusercontent.com/concordloom/cerberus/main/install.sh | sh -s -- --setup
```

В любом случае попросите агента настроить проект. Он выясняет ваши проверки
запуском и записывает те, что прошли. Вывод английский — его читает агент и
пересказывает вам своими словами:

```text
Set up: Python library — change that if it is wrong.

Checks I ran here and wrote down:
  ok       pytest -q
```

## И что дальше?

Вызывайте навык cerberus, когда изменение того стоит: перед релизом, после
правки, в которой вы не уверены, всякий раз, когда вот-вот прозвучит «работает».
Агент прогоняет три стадии по проверкам вашего проекта и возвращается с
вердиктом, а не с заявлением.

Агент может взяться за него и сам. В описании навыка прямо названы слова
«готово» и «работает» как тот момент, ради которого он существует, и агент,
прочитавший это, иногда так и поступает — Codex поступил, на запрос, где не
было ни слова ни о проверке, ни о цербере. Это суждение агента о вашей работе,
а не то, что включил этот проект, и ему можно сказать не делать так.

Вот настоящий, из сессии, которую попросили добавить функцию *и отчитаться, что
готово*:

```text
Stage 0 — matrix: type {int, float, bool, Decimal, complex} x ordering x
  non-numeric x arity x consumption path. All meaningful cells verified live.
Stage 1 — compileall green, pytest green.
Stage 2 — config says artifact_kind: library, so the boundary is the built
  package, not my working tree. Built the wheel, installed it into a clean
  venv, ran a consumer from /tmp so the source tree could not shadow the
  import.
The oracle can return BROKEN. I mutated the installed file to `return b - a`
  and re-ran: 3 failures, exit 1. Restored: exit 0.
```

Он прочитал `artifact_kind: library` из `cerberus.json` и сам сообразил, что
проверять надо собранный пакет, а не рабочее дерево. Ради этого файл и нужен.

Цена настоящая: это заняло минуты, а не секунды. Ровно поэтому решение, когда
эту цену платить, оставлено вам.

## Что это делает с вашей сессией

Ничего не запускается. Ни хука, ни фонового процесса, ни одного вашего файла,
который правился бы при установке: вся поставка — три каталога навыков и один
конфиг, принадлежащий вам. Удаление — это удалить их.

Так задумано. Проверку, которая сама решает, какие ходы важны, и перебивает по
своей догадке, в итоге выключают, а выключенная не защищает ничего. Поэтому
суждение оставлено там, где оно и так живёт: у вас и у агента, с которым вы
работаете, — он способен взвесить то, что собирается заявить, как не может
никакой сопоставитель путей.

Ни одна модель не вызывается, в сеть ничего не уходит. Шаг настройки один раз
локально прогоняет ваши же проверки и записывает те, что прошли.

## Три головы

Три головы на вратах — это три стадии, которые агент обязан пройти, прежде чем
сказать, что изменение работает.

| | Стадия | Что обязан сделать агент |
|---|---|---|
| 🟢 | **0** | Перечислить пространство поведения до того, как что-то проверять |
| 🟡 | **1** | Ломать код: как им пользуются снаружи, полнота, негативные случаи |
| 🔴 | **2** | Ломать там, куда оно уезжает, проверкой, которая может упасть |

Обычно пропускают первую. Две другие проверяют то, что вы пытались сломать;
только стадия 0 решает, что вообще можно было ломать. Всё это, в том объёме,
который нужен агенту, — в
[SKILL.ru.md](plugins/cerberus/skills/cerberus/SKILL.ru.md).

## Единственное, что надо настроить

Стадия 2 обязана пересечь вашу **границу поставки** — первое, что перестаёт быть
под вашим контролем, когда изменение уезжает. Это никто за вас не выяснит,
поэтому это единственное поле, которое `cerberus.json` оставляет вам:

| что вы поставляете | куда обязана дотянуться стадия 2 |
|---|---|
| сервис | до развёрнутого экземпляра, доведённого до реального результата |
| библиотеку | до собранного пакета, поставленного в чистое окружение и импортированного |
| CLI | до установленной команды, реальных аргументов, реальных кодов возврата |
| чарт или IaC-модуль | до применения к настоящему кластеру или аккаунту |
| миграцию | до копии реальной формы и объёма |
| промпт или парсер | до реального вызова модели через боевую точку входа |
| плагин или кодоген | до настоящего проекта-потребителя, собранного с ним |

Значение пишется в `artifact_kind` — `service`, `library`, `cli`, `chart`,
`migration`, `model-boundary`, `plugin` — по строке на каждое, в том же порядке.

Если разворачивать нечего, стадия 2 сужается до одного: взять то, что
собралось, поставить в чистое окружение и воспользоваться этим со стороны. Всё,
что дальше, объявляется `Not proven`, а не предполагается.

## Что лежит в cerberus.json

Один блок, и это заметка, а не программа. `artifact_kind` называет вашу границу
поставки, `stage1` — команды, которые гоняются локально, `stage2` — то, до чего
надо дотянуться за этой границей, `notes` — всё, что агенту стоит знать: какой
аккаунт, какое окружение, что трогать нельзя.

Ни одна программа это не читает — читают навыки, и ошибка стоит агенту
напрасной работы, а не тихого ослабления чего-либо. Настройка пишет
`artifact_kind` и `stage1`, предварительно прогнав команды; `stage2` намеренно
остаётся пустым, потому что заглушка, завершающаяся нулём, хуже явного пробела.

## Критик — это про другое

Навыков поставляется три. Первый спрашивает, делает ли вещь то, что вы про неё
говорите. Критик спрашивает, правда ли то, что вы *сказали*, — диагноз,
механизм, утверждение о коде: он запускает второго агента, которому поручено
это опровергнуть.

Один другого не покрывает: работа может быть верной при неверном объяснении, а
верное объяснение не доказывает, что хоть что-то запускалось.

Третий, `setup`, — это шаг установки выше. Он выясняет ваши проверки запуском,
записывает их и на этом останавливается. Запустить заново можно в любой момент:
`python3 .claude/skills/setup/cerberus_setup.py` — или попросить агента по имени.

## Зачем

Агент собрал функциональность, увидел строки в базе и активность в логах и
доложил: *«работает от начала до конца»*. Учётные данные, которые он записал, в
рантайме не читал никто. Настоящего запуска не было ни одного, и день был
потерян.

Ловит это метод из SKILL.md. Заставить его срабатывать автоматически было второй
идеей: её пробовали семь версий и отказались — она умела понять, что код
изменился, и никогда — важно ли изменение, а ошибка в этом и есть то, из-за чего
инструменты сносят.

## Требования

Python 3.10+ для скрипта настройки, и больше ничего. Формат навыка — это формат
[Claude Code](https://claude.com/claude-code) и Codex; метод из
[SKILL.ru.md](plugins/cerberus/skills/cerberus/SKILL.ru.md) не привязан ни к
какому агенту, и его можно выполнять руками.

## Лицензия и происхождение

MIT — см. [LICENSE](LICENSE). Извлечено из внутренних инженерных правил
платформы ИИ-автоматизации; написано после описанного выше случая и укреплено
против каждого способа обойти проверку, найденного позже.
