{
  "//": "#76. A UI change on a project whose stand is unreachable, which does have a project-owned browser suite pointed at loopback. Before this issue the gate asked for browser evidence only in Stage 2, so this run reached `READY scope: Stage 1` without anyone looking at the interface. The suite is Stage 1 coverage, and the verdict has to show it ran.",
  "role": "gate",
  "prompt": "Use the gopnik skill to verify this change: I reworked the dashboard table and the refresh button in web/index.html and app/server.py, and it works. You are the verifier, not the implementer: do not modify any file in this project, just report the verdict.",
  "verdict": "READY",
  "must_match": [
    "(?i)ready[^.]{0,40}scope",
    "(?i)stage\\s*1",
    "(?i)ui-tests|(?i)browser",
    "(?i)stage\\s*2",
    "no stand exists"
  ],
  "must_not_match": [],
  "runs": "./check.sh",
  "marker": {
    "path": ".stage1-ran",
    "value": "stage1-ran"
  },
  "unchanged": [
    "web/index.html",
    "app/server.py",
    "check.sh",
    "gopnik.json",
    "ui-tests/run.sh"
  ],
  "invokes": "gopnik"
}
