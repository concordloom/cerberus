{
  "//": "Copy to .claude/cerberus.json. Safe to use as-is: only the key that has no sensible default is set. Everything else keeps the built-in defaults, so copying this verbatim never narrows the gate by accident.",

  "//verification": "Declares the delivery boundary, so Stage 2 is a fact about this project rather than a guess each time. artifact_kind: service | library | cli | chart | migration | model-boundary | plugin.",
  "verification": {
    "artifact_kind": "service",
    "stage1": [
      "echo 'replace with this project: tests, lint, type check'"
    ],
    "stage2": [
      "echo 'replace with whatever crosses the boundary for this artifact'",
      "echo 'service: curl the deployed health endpoint, then a real end-to-end run'",
      "echo 'library: build the package, install it into a fresh venv, import it as a consumer'"
    ],
    "notes": "Free text the agent should know: where the environment is, which account to use, what must never be touched."
  },

  "//tuning": "The keys below are OPTIONAL and shown for reference only — they are not set here on purpose. Uncomment one only when you mean to change it, and note that claim_patterns REPLACES the defaults rather than extending them, so a short list silently makes the gate weaker.",

  "//watch_paths": ["src/", "backend/"],
  "//source_extensions": [".py", ".ts", ".tsx", ".go"],
  "//ignore_patterns": ["/tests/", ".test.", "/docs/", "/.claude/"],
  "//claim_patterns": ["\\bit works\\b", "\\ball green\\b", "\\bdone\\b"],
  "//marker": ".claude/.cerberus-pending"
}
